enum KeyAction {
  backSpace,
  enter,
  shift,
  space,
  symbols,
  symbolsAlt,
  switchLanguage,
}

enum KeyType {
  action,
  character,
}

enum KeyboardType {
  text,
  number,
  phone,
  custom,
}

enum LayoutStage {
  primary,
  secondary,
  tertiary,
}
