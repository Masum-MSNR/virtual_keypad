name: Release & Publish

on:
  push:
    branches: [ main ]  # Triggers the "Tagging" job
    tags: [ 'v*' ]      # Triggers the "Publishing" job

jobs:
  # JOB 1: Create the tag (Runs only on branch push)
  create-tag:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.RELEASE_PAT }} # <--- IMPORTANT: Use PAT here to checkout
          fetch-depth: 0

      - name: Read version
        id: version
        run: |
          VERSION=$(grep '^version:' pubspec.yaml | awk '{print $2}')
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Create and Push Tag
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          
          TAG="v${{ steps.version.outputs.version }}"
          
          if git rev-parse "$TAG" >/dev/null 2>&1; then
            echo "Tag $TAG already exists. Skipping."
          else
            git tag $TAG
            git push origin $TAG
          fi
          # Because we used RELEASE_PAT in checkout, this push will trigger the job below automatically.

  # JOB 2: Publish to Pub.dev (Runs only on tag push)
  publish:
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write # Required for Pub.dev OIDC
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Setup Pub Credentials
        uses: dart-lang/setup-dart@v1

      - name: Publish
        run: flutter pub publish --force